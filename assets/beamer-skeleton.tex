% !TEX program = xelatex
% ============================================================
% paper-to-podium Beamer skeleton
% Theme: SimplePlus (pm25/SimplePlus-BeamerTheme, ~280 stars on
% GitHub) — a sober dark-blue academic theme. It is NOT on CTAN,
% so the four .sty files are bundled in assets/simpleplus/ (public
% domain / Unlicense, (c) Pin-Yen Huang); keep that folder next to
% this file when you copy the skeleton into a project.
%
% Compile with XeLaTeX. BEFORE writing content, compile this file
% unchanged and LOOK at the output: xelatex can exit 0 while silently
% dropping CJK glyphs, so "it compiles" is not enough — check that the
% characters actually rendered.
% ============================================================
\documentclass[aspectratio=169,11pt]{beamer}
\usetheme{SimplePlus}
\usepackage{graphicx,booktabs}

% ---------- Latin fonts ----------
% Times for body text keeps numbers looking professional. fontspec must be
% loaded explicitly — without it, \setmainfont is undefined and its argument
% gets typeset as stray text at the top of the title page. Do NOT load
% newtxtext/newtxmath here: loading them BEFORE ctex breaks later CJK
% font lookup with "Transform components aren't all known" errors.
\usepackage{fontspec}
\setmainfont{Times New Roman}

% ---------- CJK decks: UNCOMMENT the whole block below ----------
% Verified on MiKTeX + Windows. The critical pieces are:
%   * fontset=none — ctex's default fontset demands SimSun/Fandol fonts
%     that many MiKTeX installs cannot load, producing a deck that
%     compiles fine but shows empty boxes instead of Chinese;
%   * explicit \setCJKmainfont afterwards.
% \usepackage[fontset=none]{ctex}
% \setCJKmainfont[BoldFont={Microsoft YaHei Bold}]{Microsoft YaHei}
% \setCJKsansfont[BoldFont={Microsoft YaHei Bold}]{Microsoft YaHei}
% (On macOS use "PingFang SC" / "PingFang SC Semibold"; on Linux use
% "Noto Sans CJK SC". Keep BoldFont set — frame titles are bold.)
% ----------------------------------------------------------------

% ---------- two helpers every deck ends up wanting ----------
% \hl      highlight the one number that matters, in the theme blue
% \srcnote tiny gray source line under figures/tables
\newcommand{\hl}[1]{{\color{MediumBlue}\bfseries #1}}
\newcommand{\srcnote}[1]{{\tiny\color{gray} #1}}

\title[Short Title]{Full Title of the Talk}
\subtitle{A Paper Presentation}
\author[Author]{Author Name}
\institute[Institute Short]{Institute Full Name}
\date{\today}

\begin{document}

\maketitle

\begin{frame}{Outline}
  \tableofcontents
\end{frame}

\section{Background}

% ---- pattern 1: content page — the load-bearing sentences, full width ----
\begin{frame}{One Idea Per Page}
  \begin{itemize}
    \setlength{\itemsep}{6pt}
    \item The single most important sentence from this stretch of the script,
          with its key number \hl{highlighted}.
    \item At most five or six short items. If you need more, this is two pages.
    \item Enough substance that the speaker can glance at the screen and
          remember what comes next — the deck is the speaker's prompter too.
  \end{itemize}
  \vfill
  \srcnote{Source: where the numbers on this page come from.}
\end{frame}

% ---- pattern 2: figure page — big figure + secondary script text beside ----
% The figure is what the audience studies; the side text is what the speaker
% reads aloud meanwhile (mark it :::verbatim in the script).
\begin{frame}{Figure Page}
  \begin{columns}[T]
    \column{0.42\textwidth}
      {\small Secondary material: context and caveats that didn't make the
       key-points cut. Smaller font is fine here — it is the speaker's text,
       shown on screen while the audience looks at the figure.\par}
    \column{0.55\textwidth}
      \centering
      % \includegraphics[width=\linewidth]{figure}
      \fbox{\parbox[c][0.55\textheight][c]{0.9\linewidth}{\centering figure}}
      \\[2pt]
      \srcnote{What the figure shows, and where it is from.}
  \end{columns}
\end{frame}

\section{Results}
% ...

% Sparse frames are vertically centered by default and \vfill does not
% stretch there — position content with explicit spacing (\\[2em], \vspace{}).
\begin{frame}{Thank You}
  \centering\Large Thank you!
\end{frame}

\end{document}
